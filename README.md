# PartyGameKit

Reusable multiplayer foundation for shared-screen party games where a TV/browser acts as the common game screen and phones act as player controllers.

The project is being designed to support both local LAN play and optional cloud/backend-assisted networking without coupling game logic to a specific transport.

Architecture and roadmap are being documented in `docs/`.
